# نظام الحضور والانصراف الشامل (Attendance System)

نظام ويب متكامل لإدارة الموارد البشرية والمخزون والمحاسبة، مبني بإطار Django مع دعم ثنائي اللغة (عربي/إنجليزي) وواجهة متجاوبة للمحمول وسطح المكتب.

## الإصدار
**الإصدار الحالي: 3.9**

## المتطلبات
- Python 3.9+
- PostgreSQL 16+ (أو MySQL كخيار بديل)
- مكتبات Python المذكورة في `requirements.txt`

### المتطلبات الأساسية
```bash
pip install -r requirements.txt
```

## التشغيل السريع

### باستخدام Docker (موصى به)
```bash
docker-compose up -d
```

### التشغيل اليدوي
```bash
python manage.py migrate
python manage.py runserver
```

بعد التشغيل:
- واجهة تسجيل الحضور/الانصراف: http://127.0.0.1:8000/
- لوحة تحكم المدير: http://127.0.0.1:8000/admin-login/
- نظام الرواتب: http://127.0.0.1:8000/payroll/

### حساب المدير الافتراضي
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

## المميزات الرئيسية

### 1. نظام الحضور والانصراف
- تسجيل الحضور والانصراف برقم المستخدم فقط
- التحقق من الموقع الجغرافي (GPS) مع نطاق قابل للتعديل لكل فرع
- دعم الفروع المتعددة مع إحداثيات منفصلة
- التحقق من عنوان IP المسموح به لكل فرع
- تسجيل تلقائي للوصول والسجلات

### 2. إدارة المستخدمين
- إضافة وتعديل وحذف المستخدمين
- تعيين الفروع المسموحة لكل مستخدم
- إدارة أجهزة المستخدمين (Device Tokens)
- تعيين تاريخ بداية التوظيف
- إدارة مراجعي الأوامر (Orders Reviewers)

### 3. إدارة الفروع
- إضافة وتعديل وحذف الفروع
- تحديد إحداثيات GPS لكل فرع
- تحديد نطاق الموقع (بالمتر) لكل فرع
- تحديد نطاقات IP المسموح بها (CIDRs)

### 4. نظام المخزون
- **إدارة المنتجات**: إضافة وتعديل وحذف المنتجات مع أكواد وأسعار
- **إدارة الفئات**: تنظيم المنتجات في فئات
- **إدارة الموردين**: إدارة بيانات الموردين وكشوف الحساب
- **إدارة العملاء**: إدارة بيانات العملاء وكشوف الحساب
- **جرد المخزون**: عمليات جرد المخزون الدورية

### 5. نظام المبيعات والمشتريات
- **أوامر الشراء (Purchase Orders)**:
  - إنشاء وتعديل أوامر الشراء
  - إضافة البنود والكميات
  - إدارة الدفعات وطرق الدفع
  - طباعة أوامر الشراء
  - إدارة مرتجعات الشراء

- **أوامر البيع (Sales Orders)**:
  - إنشاء وتعديل أوامر البيع
  - إدارة الدفعات وطرق الدفع
  - طباعة إيصالات البيع
  - إدارة مرتجعات البيع

### 6. نظام المحاسبة
- **إدارة المبيعات**: تسجيل وتتبع المبيعات اليومية
- **إدارة المصاريف**: تسجيل المصاريف وتصنيفها
- **إدارة الضرائب**: إدارة أنواع الضرائب ونسبها
- **أهداف المستخدمين**: تعيين وتتبع أهداف المبيعات لكل مستخدم
- **إدارة الفواتير**: رفع ومراجعة الفواتير
- **الصندوق (Cashbox)**: إدارة عمليات الصندوق

### 7. التقارير
- **تقارير المحاسبة**:
  - تقرير يومي
  - تقرير شهري
  - تقرير سنوي
- **تقارير الرواتب**:
  - تقرير الرواتب اليومي
  - تقرير الرواتب الشهري
- **تصدير البيانات**:
  - تصدير السجلات إلى Excel
  - تصدير كشوف الحساب
  - تصدير تقارير متنوعة

### 8. نظام الرواتب المتقدم
- **إدارة الفترات (Payroll Periods)**:
  - إنشاء فترات رواتب شهرية
  - إغلاق الفترات وحساب الرواتب
  - مراجعة وتعديل سجلات الرواتب

- **إدارة رواتب الموظفين**:
  - تعيين الرواتب الأساسية
  - إدارة معدلات الساعة
  - إدارة التأمينات
  - إضافة التعديلات (الخصومات والإضافات)

- **حساب الرواتب**:
  - حساب تلقائي بناءً على سجلات الحضور
  - دعم الساعات الإضافية
  - حساب الخصومات والإضافات
  - إصدار كشوف الرواتب (Payslips)

- **مراجعة الأوامر**:
  - مراجعة أوامر البيع قبل الموافقة
  - نظام موافقات متعدد المستويات

### 9. إدارة الأجهزة والأمان
- **إدارة Device Tokens**: تسجيل الأجهزة المسموح بها
- **سجلات الوصول (Access Logs)**: تتبع جميع محاولات الوصول
- **سجلات التدقيق (Audit Logs)**: تتبع جميع التعديلات
- **التحقق من الشبكة**: تحديد نوع الشبكة (WiFi/Cellular)

### 10. الميزات الإضافية
- **دعم ثنائي اللغة**: عربي (افتراضي) وإنجليزي
- **مساعد ذكي (AI Chat)**: نظام أسئلة وأجوبة تلقائي
- **نظام إدارة المعرفة (FAQ)**: قاعدة بيانات للأسئلة الشائعة
- **تتبع المشاكل (Support Issues)**: تسجيل وحل المشاكل المتكررة
- **النسخ الاحتياطي**: نظام نسخ احتياطي تلقائي للبيانات
- **تعدد المستأجرين (Multi-tenancy)**: دعم عملاء متعددين

### 11. التصدير والطباعة
- تصدير جميع البيانات إلى Excel
- طباعة أوامر البيع والشراء
- طباعة كشوف الرواتب
- طباعة الإيصالات

## هيكلية المشروع

```
attendance_system/
├── attendance/              # التطبيق الرئيسي
│   ├── models.py            # النماذج (المستخدمين، الفروع، المخزون، إلخ)
│   ├── views.py             # واجهات الحضور والانصراف
│   ├── inventory_views.py  # واجهات المخزون والمبيعات
│   ├── urls.py              # المسارات الرئيسية
│   ├── urls_payroll.py      # مسارات نظام الرواتب
│   ├── forms.py             # النماذج (Forms)
│   ├── admin.py             # لوحة Django Admin
│   ├── middleware.py        # البرامج الوسيطة
│   ├── templatetags/        # وسوم القوالب المخصصة
│   └── services/            # الخدمات المساعدة
├── clients/                 # نظام تعدد المستأجرين
├── templates/               # القوالب
│   ├── attendance/          # قوالب الحضور
│   └── base.html            # القالب الأساسي
├── static/                  # الملفات الثابتة
│   ├── img/                 # الصور والشعارات
│   └── css/                 # ملفات التنسيق
├── locale/                  # ملفات الترجمة
├── scripts/                 # السكريبتات المساعدة
├── requirements.txt         # المتطلبات
├── docker-compose.yml       # إعدادات Docker
└── manage.py               # أداة إدارة Django
```

## إعدادات قاعدة البيانات

### PostgreSQL (موصى به)
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'attendance_db',
        'USER': 'attendance_user',
        'PASSWORD': 'attendance_pass',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

### MySQL (بديل)
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'attendance_db',
        'USER': 'root',
        'PASSWORD': 'your_password',
        'HOST': 'localhost',
        'PORT': '3306',
    }
}
```

## الترجمة والتوطين

لإضافة ترجمات جديدة:
```bash
python manage.py makemessages
python manage.py compilemessages
```

## النشر

### باستخدام Docker
```bash
docker-compose up -d
```

### يدوياً
```bash
python manage.py collectstatic
python manage.py migrate
gunicorn attendance_system.wsgi:application
```

## التوثيق والمساعدة
- التوثيق الكامل: https://cyborkspider.github.io/en/
- المشروع على GitHub: https://github.com/CyborkSpider/attendance_system

## العلامة التجارية (CYBORK)
تم تهيئة المشروع باستخدام هوية CYBORK.

- الشعار الرئيسي: `static/img/cybork-logo.png`
- أيقونة المتصفح: `static/img/favicon.ico`
- Manifest: `static/site.webmanifest`

### تحديث الشعار والأيقونات
1. استبدل الملف `static/img/cybork-logo.png` بصورة PNG مربّعة عالية الدقة (يفضل 512×512)
2. توليد favicon.ico متعدد المقاسات:
   ```bash
   python scripts/generate_favicon.py
   ```
3. جمع الملفات الثابتة:
   ```bash
   python manage.py collectstatic
   ```

## الترخيص
هذا المشروع مملوك لـ CYBORK Spider.

## الدعم الفني
للدعم الفني والاستفسارات، يرجى زيارة: https://cyborkspider.github.io/en/
